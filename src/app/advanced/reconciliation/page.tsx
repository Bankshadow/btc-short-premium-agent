import ReconciliationDashboard from "@/components/advanced/ReconciliationDashboard";

export const metadata = {
  title: "Reconciliation · Advanced",
  description:
    "MVP 88 — engine consistency across analysis, mission snapshot, journals, and testnet.",
};

export default function ReconciliationPage() {
  return <ReconciliationDashboard />;
}
